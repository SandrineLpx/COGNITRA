# CLAUDE.md

This repository uses `AGENTS.md` as the canonical AI agent instruction file.

If your tooling loads `CLAUDE.md`, apply the same instructions defined in:
- `AGENTS.md`

Do not maintain duplicate instruction sets in both files. Update `AGENTS.md` only.
